{
  "name": "digital-recipes",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted bilingual recipe site (static site generator).",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "validate": "node validate.js recipes/*.json"
  },
  "dependencies": {
    "sharp": "^0.34.0"
  }
}
